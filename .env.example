# Copy to .env locally. Never commit real credentials.

# Orchestration
N8N_BASE_URL=
N8N_WEBHOOK_BASE_URL=

# Durable state (future / optional until selected)
DATABASE_URL=
REDIS_URL=

# Media storage (future / optional until selected)
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_BUCKET=
OBJECT_STORAGE_ACCESS_KEY=
OBJECT_STORAGE_SECRET_KEY=

# Generation providers
RUNWAY_API_KEY=

# Add other provider/platform credential variable names only when they are actually introduced.
