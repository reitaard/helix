# Services

Do not create services just to match a speculative architecture.

Custom code should appear here when a real boundary is clear—for example a provider-neutral job API, durable project state, media ownership, analytics ingestion, or an interface needed by Reitaard that is awkward to implement safely in n8n alone.
