# Workflows

Store n8n exports and workflow-specific documentation here.

Suggested naming:

- `research-*.json`
- `production-*.json`
- `publishing-*.json`
- `analytics-*.json`
- `video-worker-*.json`

Secrets, API keys, signed media URLs, and credentials must never be committed.
