# Distribution

Future home for supported publishing/scheduling logic and platform adapters.

Distribution should consume approved media + metadata and produce a durable `PublishedPost` identity that Analytics can reference.

Platform-specific permissions, quotas, account requirements, posting rules, and integrity constraints must be validated when implementation begins.